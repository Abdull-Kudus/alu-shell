#!/usr/bin/env bash
# Loop from 1 to 10, special outputs on 4 and 8 using if, elif, else
i=1; while [ "$i" -le 10 ]; do [ "$i" -eq 4 ] && echo "bad luck" || ([ "$i" -eq 8 ] && echo "good luck" || echo "Best School"); i=$((i + 1)); done
